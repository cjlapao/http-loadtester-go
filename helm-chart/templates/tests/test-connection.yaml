apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-loadtester.fullname" . }}-test-connection"
  labels:
    {{- include "http-loadtester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "http-loadtester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
