apiVersion: v1
kind: Service
metadata:
  name: {{ include "http-loadtester.fullname" . }}
  labels:
    {{- include "http-loadtester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "http-loadtester.selectorLabels" . | nindent 4 }}
